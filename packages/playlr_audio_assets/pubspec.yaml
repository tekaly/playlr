name: tekaly_playlr_audio_assets
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ">=5.0.0"
  test: ">=1.24.0"
  process_run: ">=1.2.1+1"
resolution: workspace
